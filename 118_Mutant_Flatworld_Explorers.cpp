#include <bits/stdc++.h>
using namespace std;

int main() {
    int len, wid;
    cin >> len >> wid;
    int x, y;
    char c;
    char side[4] = {'N', 'E', 'S', 'W'};
    while(cin >> x >> y >> c) {
        string move;
        getline(cin, move);
        for(int i=0; i<move.size(); i++) {
            if(move[i]=='L') {
            }
        }
    }
}